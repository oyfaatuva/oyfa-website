import styles from './Loading.module.css';

/* Current no actual styled Loading component just an empty div to fill the space */
export default function Loading () {
    return (
        <div className={styles.loading}>
        </div>
    );
}